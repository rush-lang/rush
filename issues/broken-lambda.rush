## lambda expressions are broken since the change to
## patterns for function parameters. Named patterns
## require a root declaration to hook onto in order
## to determine what exactly it is naming.
## currently there are no suitable parent declaration types
## for parameters. One may need to be introduced to fix this
## issue.

func main:
	let fn = (x: int) => x