name: macos-clang

permissions:
  contents: read

on: [push, pull_request]

jobs:
  macos-clang:
    uses: ./.github/workflows/build-test.yml
    with:
      runs-on: macos-latest
      compiler: clang
    secrets: inherit
