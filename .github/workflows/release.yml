on:
  workflow_call:
    inputs:
      runs-on:
        type: string
        description: 'Operating System'
        required: true
        default: 'ubuntu-latest'

permissions:
  contents: read

jobs:
  release:
    runs-on: ${{ inputs.runs-on }}
    steps:
      - shell: bash
        run: echo "Hello World"

