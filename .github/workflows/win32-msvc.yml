permissions:
  contents: read

on: [push, pull_request]

jobs:
  build-test:
    uses: ./.github/workflows/build-test.yml
    with:
      runs-on: windows-latest
      compiler: msvc
    secrets: inherit
