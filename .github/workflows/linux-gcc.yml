name: linux-gcc

permissions:
  contents: read

on: [push, pull_request]

jobs:
  linux-gcc:
    uses: ./.github/workflows/build-test.yml
    with:
      runs-on: ubuntu-latest
      compiler: gcc
    secrets: inherit
